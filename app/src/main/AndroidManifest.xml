<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Shake Detection Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Background Location (Optional - only if you need background alerts) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finalproject"
        tools:targetApi="31">

        <!-- Google Play Services metadata -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- ==================== SHAKE DETECTION SERVICE ==================== -->
        <!-- Shake Detector Service - Monitors accelerometer for emergency shakes -->
        <service
            android:name=".ShakeDetectorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Boot Receiver - Restarts shake detection after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- ================================================================= -->

        <!-- Activities -->
        <activity
            android:name=".OfflineAlertsActivity"
            android:exported="false" />
        <activity
            android:name=".finishfile"
            android:exported="false" />

        <!-- Launcher Activity -->
        <activity
            android:name=".getstarted"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity
            android:name=".createaccount_email"
            android:exported="false" />
        <activity
            android:name=".createaccount_phone"
            android:exported="false" />
        <activity
            android:name=".signin_email"
            android:exported="false" />
        <activity
            android:name=".signin_phone"
            android:exported="false" />
        <activity
            android:name=".otp"
            android:exported="false" />

        <!-- Main App Activities -->
        <activity
            android:name=".dashboard"
            android:exported="false" />
        <activity
            android:name=".travel_safety"
            android:exported="false" />
        <activity
            android:name=".personal_safety"
            android:exported="false" />
        <activity
            android:name=".medical_safety"
            android:exported="false" />
        <activity
            android:name=".gotosettings"
            android:exported="false" />
        <activity
            android:name=".emergency_type"
            android:exported="false" />
        <activity
            android:name=".add_emergency_contact"
            android:exported="false" />
        <activity
            android:name=".emergency_contacts"
            android:exported="false" />
        <activity
            android:name=".alerthistory"
            android:exported="false" />
        <activity
            android:name=".finishpage"
            android:exported="false" />
    </application>

</manifest>
